import { Button, ButtonText, Letter } from "./styled";

const MailButton = () => {

    return (
        <Button>
            <ButtonText>
                <Letter />
                Hire Me
            </ButtonText>
        </Button>
    );
};

export default MailButton;